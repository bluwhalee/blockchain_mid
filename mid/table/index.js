function check(){
 

    for(var i=1;i<=10;i++)
    {
        let result = eval(4*i)
        console.log(`${4} * ${i} = ${result}`);
    }
    for(var i=1;i<=10;i++)
    {
        let result = eval(5*i)
        console.log(`${5} * ${i} = ${result}`);
    }
        
        
    
    
}